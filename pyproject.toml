[project]
name = "tsa-server"
version = "0.1.0"
description = "Minimal RFC3161 Time Stamping Authority server"
requires-python = ">=3.11"

[project.scripts]
tsa-server = "tsa.server:main"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
